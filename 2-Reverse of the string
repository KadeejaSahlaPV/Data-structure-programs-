#include<stdio.h>
#include<string.h>
int main()
{
    char str[50],*s;
    int len,i;
    printf("Enter the string:");
    gets(str);
    s=str;
    len=strlen(s);
    printf("The reverse of the string is:");
       for(i=len;i>=0;i--)
          printf("%c",*(s+i));
}
